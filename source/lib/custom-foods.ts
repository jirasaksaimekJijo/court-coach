import type {FoodPreset} from './food-presets';
export const customFoodKey='court-coach:custom-foods:v1';
export function validCustomFood(f:any):f is FoodPreset{return !!f&&typeof f.id==='string'&&/^custom-[a-f0-9-]{36}$/.test(f.id)&&typeof f.name==='string'&&!!f.name.trim()&&f.name.length<=120&&typeof f.unit==='string'&&!!f.unit.trim()&&f.unit.length<=40&&Array.isArray(f.ingredients)&&f.ingredients.every((i:any)=>typeof i==='string'&&i.length<=1000)&&typeof f.method==='string'&&f.method.length<=2000&&!!f.values&&['calories','protein','carbs','fat'].every(k=>f.values[k]===null||typeof f.values[k]==='number'&&Number.isFinite(f.values[k])&&f.values[k]>=0&&f.values[k]<=(k==='calories'?10000:1500))}
export function readCustomFoods(storage:Pick<Storage,'getItem'>){const items=JSON.parse(storage.getItem(customFoodKey)||'[]');if(!Array.isArray(items)||!items.every(validCustomFood))throw Error('Saved menu items could not be read. Existing data has not been changed.');return items as FoodPreset[]}
export function addCustomFood(storage:Pick<Storage,'getItem'|'setItem'>,food:FoodPreset){if(!validCustomFood(food))throw Error('Check the menu name, serving and nutrition values.');const current=readCustomFoods(storage);if(current.length>=200)throw Error('The menu library is limited to 200 custom items.');const next=[...current,food];storage.setItem(customFoodKey,JSON.stringify(next));return next}

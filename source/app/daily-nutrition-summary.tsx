'use client';
import {nutritionReview} from '@/lib/nutrition-review';
import type {Profile,Entry} from '@/lib/coach';
const labels={calories:'พลังงาน',protein:'โปรตีน',carbs:'คาร์บ',fat:'ไขมัน'};
export default function DailyNutritionSummary({profile,logs,date,entry}:{profile:Profile;logs:Record<string,Entry>;date:string;entry:Entry}){
 const review=nutritionReview(profile,logs,date,entry),below=review.rows.filter(r=>r.delta!==null&&r.delta<0).map(r=>labels[r.key]),above=review.rows.filter(r=>r.delta!==null&&r.delta>0).map(r=>labels[r.key]);
 return <section className="daily-nutrition-summary"><h4>อาหารวันนี้ · กินเทียบกับเป้า</h4><p>{review.energyMismatch?'ยอดแคลอรีกับสารอาหารต่างกันมาก ควรตรวจยอดก่อนสรุป':review.complete?'ยืนยันบันทึกอาหารครบวันแล้ว':'ข้อมูลอาหารยังไม่ครบหรือยังไม่ยืนยันครบวัน · ยอดนี้อาจยังเป็นเพียงบางมื้อ'}</p>{review.complete&&<p><b>{below.length?`${below.join(' / ')} ยังต่ำกว่าเป้า`:'ถึงหรือเกินเป้าทั้ง 4 รายการ'}</b>{above.length?` · ${above.join(' / ')} สูงกว่าเป้า`:''}</p>}{review.energyMismatch&&<p className="notice">โปรตีน คาร์บ และไขมันที่กรอกคิดเป็นพลังงานประมาณ {review.macroCalories} kcal แต่กรอกพลังงานรวม {entry.calories} kcal ตัวเลขอาจไม่ตรงเป๊ะจากฉลากและใยอาหาร แต่ส่วนต่างนี้มาก ควรตรวจว่ากรอกครบและใช้หน่วยเดียวกัน ระบบยังไม่แก้ตัวเลขให้เอง</p>}<div className="nutrition-review-grid">{review.rows.map(r=>{const unit=r.key==='calories'?'kcal':'g';return <article key={r.key}><b>{labels[r.key]}</b><p><strong>{r.actual??'—'}</strong> / {r.target} {unit}</p><span className={r.delta===null?'muted':r.delta>0?'above-target':r.delta<0?'below-target':'at-target'}>{r.actual===null?'ยังไม่ได้กรอก':r.missing?'ยังมีมื้อที่ไม่ได้กรอกค่านี้':r.delta===0?'ตรงเป้า':r.delta!<0?`ต่ำกว่าเป้า ${Math.abs(r.delta!)} ${unit}`:`เกินเป้า ${r.delta} ${unit}`}</span></article>})}</div><p className="muted">{review.snapshot?'เทียบกับเป้าที่เก็บไว้ตอนบันทึกวันนั้น':'วันเก่านี้ยังไม่มีเป้าที่เก็บไว้ จึงเทียบกับเป้าคำนวณจากการตั้งค่าปัจจุบัน ตัวเลขเปรียบเทียบอาจต่างจากวันที่ซ้อมจริง'} · “ต่ำกว่าเป้า” หมายถึงต่างจากแผน ไม่ใช่การวินิจฉัยว่าขาดสารอาหาร</p><p className="muted">ถ้ายังมีมื้อเหลือ ให้ดูสารอาหารที่ต่ำกว่าเป้าประกอบการเลือกมื้อถัดไป หากจบวันแล้วไม่ต้องอดหรือออกกำลังชดเชยยอดที่เกินในวันถัดไป</p></section>
}
